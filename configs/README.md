# Configs

Store experiment and pipeline configuration files here.

Suggested pattern:
- one config file per experiment or variant
- include experiment ID in config filename when relevant
- document key parameters at the top of each config

Examples:
- `exp_001_baseline.yaml`
- `exp_002_poison_authority_seed_01.yaml`
