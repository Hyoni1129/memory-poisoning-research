# Raw Data

Place untouched source data here.

Rules:
- no manual edits to files in this directory
- keep provenance notes in `data/README.md` or matching experiment metadata
- if data is external, document source URL and access date
