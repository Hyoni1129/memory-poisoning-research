# Final Data

Store curated datasets used directly by experiments.

Rules:
- tie each dataset snapshot to a config or experiment ID
- avoid ad-hoc manual edits without documentation
- track schema assumptions in experiment metadata
