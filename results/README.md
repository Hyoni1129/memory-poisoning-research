# Results

Store generated outputs from experiments.

- `tables/`: numeric summaries and CSV/Markdown tables
- `figures/`: plots and visualizations
- `logs/`: run logs and console captures

Every result should map to an experiment ID and a config.
